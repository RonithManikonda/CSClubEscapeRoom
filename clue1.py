# The file input output is done for you
with open("Inputs/input1.txt", "r") as file:
    # each element in strings is a line from the input file
    strings = [line.strip() for line in file]

# Your code starts here
